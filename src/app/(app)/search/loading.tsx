import { AppRouteSkeleton } from "@/components/shared/app-route-skeleton";

export default function SearchLoading() {
  return (
    <AppRouteSkeleton title="Search" description="Find anything across IMX OS" />
  );
}
